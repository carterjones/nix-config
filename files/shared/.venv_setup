#!/bin/bash

# Set up virtualenvwrapper.
if [[ $(uname) == Linux* ]]; then
    eval "$(pyenv init -)"
    eval "$(pyenv virtualenv-init -)"
elif [[ $(uname) == Darwin* ]]; then
    eval "$(pyenv init -)"
    pyenv virtualenvwrapper
fi
