#!/bin/bash

# Set up pyenv, virtualenv, and virtualenvwrapper.
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"
VIRTUALENVWRAPPER_PYTHON=$(pyenv which python)
export VIRTUALENVWRAPPER_PYTHON
pyenv virtualenvwrapper
